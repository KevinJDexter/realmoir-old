const locations = [];
const events = [];

$(document).ready(onReady);

function onReady () {
  let Falor = new Character ('Falor Fleetwind', '198 Argas', '220 Argas', '22 (deceased)', 'Fleetwind Manor');
  characterList.push(Falor);
  console.log(characterList);
}

